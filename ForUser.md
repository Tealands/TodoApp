# 非エンジニア向け取扱説明書

このファイルは、プログラムの専門知識がない方でも `TodoApp` を使えるように作成した手順書です。できるだけやさしい言葉で説明しています。

## 1. 使う前の準備

- このアプリは Windows で使うことを想定しています。
- 必要なソフトウェアやセットアップは、開発者や担当者にお願いしてください。
- すでに用意されている場合は、次の「起動方法」に進んでください。

## 2. 起動方法

1. `TodoApp` のフォルダーを開きます。
2. `launch_todo.pyw` をダブルクリックします。
   - 画面に何も表示されずに起動する場合がありますが、裏でアプリが立ち上がっています。
3. 自動的にブラウザが開き、Todo管理画面が表示されます。

> もしブラウザが開かない場合は、`app.py` が動いているか、起動に必要なソフトが入っているかを担当者に確認してください。

## 3. 最初の設定

アプリを初めて使うときは、次のどちらかを選びます。

### 既存のデータベースを使う

- すでにある Access データベースファイルを使う場合
- `参照` ボタンを押し、`*.accdb` または `*.mdb` ファイルを選びます
- 選択後に `このDBを使う` を押します

### 新しいデータベースを作る

- 新しくタスクを保存するファイルを作る場合
- `保存先フォルダー` を `参照` で選びます
- `ファイル名` を入力します（初期値のままで問題ありません）
- `このフォルダーに作成` を押します

## 4. Todo の使い方

### ASAPゾーン

- すぐにやる必要があるタスクを登録します。
- `ASAPでやることを入力` に内容を入れて、`追加` を押します。
- `授業`欄に授業名やグループを入れることもできます。

### メインリスト

- 期限付きのタスクを登録します。
- `やることを入力` に内容を入れ、必要に応じて `授業` と `期限` を設定します。
- `追加` を押すとタスクが保存されます。

### タスクの操作

- タスク名の横にあるチェックを入れると、完了済みにできます。
- `編集` ボタンでタスク内容を変更できます。
- `削除` ボタンでタスクを消せます。
- 複数タスクをまとめて削除するには、一覧のチェックを入れて `✔ ...タスクを削除` を押します。

## 5. 便利な機能

- `JSONでエクスポート` を押すと、現在のタスクをバックアップできます。
- `DB設定を変更` を押すと、データベースの設定をやり直せます。
- 画面に追加した「デスクトップにショートカット作成」ボタンを押すと、Windows のデスクトップにアプリ起動用のアイコンを作成できます。

## 6. 閉じ方

- ブラウザのタブを閉じるだけで大丈夫です。
- `launch_todo.pyw` で起動した場合、アプリは自動的に終了します。

### バックエンドの自動シャットダウンについて

- **通常運用**：ブラウザを閉じた（またはタブを閉じた）後、バックエンドは **8秒間** で自動的に停止します
  - 8秒以内にブラウザで画面を再度開いた場合、バックエンドは停止せずにそのまま動作します
  
- **起動時**：アプリを起動した直後は、ブラウザの起動を待つため **最大90秒間** 自動停止が遅延します
  - この間にブラウザを開くと、通常の8秒カウントダウンが開始されます

つまり、ブラウザを閉じるだけでバックエンドも自動的に停止するため、手動で終了させる必要はありません。

## 7. 困ったとき

- アプリが起動しない
  - `launch_todo.pyw` をもう一度ダブルクリックしてください。
  - それでも動かない場合は、担当者に `Python` や `Flask` のセットアップを確認してもらってください。
- データが消えたように見える
  - 保存先のデータベースが正しく指定されているか確認してください。
  - `DB設定を変更` で再設定できます。

---

この説明書は、Todo の基本操作とショートカット作成を含む流れをまとめたものです。必要に応じて、担当者に使い方を教えてもらってください。
